# README refine

## Status: done

## Context
The README and CLAUDE.md had accumulated minor staleness after the Catppuccin
Frappé/Latte/Macchiato schemes were added (commit `d6aff7d`) and the empty
`docs/` placeholder was removed. Quick polish pass for accuracy.

## Changes
- [x] `README.md`: scheme count and list updated `3` → `6` (Features section)
- [x] `README.md`: removed broken placeholder URL on the `claude-setup` mention
- [x] `CLAUDE.md`: scheme count updated `3` → `6` in the project description
- [x] `CLAUDE.md`: removed `Pending` section referencing the obsolete
      `docs/session-name-plan.md` (Claude Code now ships `/rename`, so the
      session-name workaround is no longer needed)
- [x] removed empty `docs/` directory
