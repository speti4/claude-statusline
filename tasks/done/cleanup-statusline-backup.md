# Clean up statusline.sh backup

## Status: done

## Description
Deleted both `~/.claude/statusline.sh` and `~/.claude/statusline.shbackup`.
The Python version (`statusline.py`) is stable and in use since statusline-v2.
